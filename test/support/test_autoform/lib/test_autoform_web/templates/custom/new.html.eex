<h1>Custom Form</h1>

<%= custom_render_autoform(@conn, :create, [
  TestAutoform.Address,
  "<h1>Between address and user</h1>",
  {TestAutoform.User, exclude: [:age, :biography]},
  {TestAutoform.User, exclude: [:name, :address], custom_labels: %{age: "Years on Earth"}}
], path: "/endpoint") %>